<div class="container">
  <%= simple_form_for [@user, @listing] do |l| %>
    <%= l.input :name %>
    <%= l.input :description %>
    <%= l.input :price %>
    <%= l.input :photo, as: :file %>
    <%= l.submit %>
  <% end %>
</div>